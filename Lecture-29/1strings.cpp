#include<bits/stdc++.h>
using namespace std;

int main(){
    // string A = "Avinash Kumar";
    // cout<<A.length()<<endl;
    // cout<<A[6]<<endl;
    // for(int i = 0; i < A.length(); i++){
    //     cout<<A[i]<<" ";
    // }
    // for(char ele : A){
    //     cout<<ele<<" ";
    // }
    // A.push_back('!');
    // cout<<A<<endl;
    // A.pop_back();
    // cout<<A;
    // string B = "QWE";
    // B.clear();
    // cout<<B<<"Empty"<<endl;
    // string s;
    // string s = "hello";

    // cin >> s;
    // getline(cin, s);

    // s[0];
    // s[s.size() - 1];

    // s.size();
    // s.length();

    // s.push_back('a');
    // s.pop_back();

    // s += "abc";

    // s.clear();
    // s.empty();

    // s == t;
    // s != t;

    // for(int i = 0; i < s.size(); i++)

    // for(char ch : s)
    // string s;
    // string s = "hello";

    // cin >> s;
    // getline(cin, s);

    // s[0];
    // s[s.size() - 1];

    // s.size();
    // s.length();

    // s.push_back('a');
    // s.pop_back();

    // s += "abc";

    // s.clear();
    // s.empty();

    // s == t;
    // s != t;

    // for(int i = 0; i < s.size(); i++)

    // for(char ch : s)

    //Concatenation
    // string s1,s2,s3;
    // s1 = "ABC";
    // s2 = "DEF";
    // s3 = s1 + s2;
    // cout<< s3;

    // 's' -> It is a character;
    // "s" -> It is a string.
    // string s = "Kartik";
    // s.pop_back();
    // s.push_back('K');
    // cout<<s;

    string s1 = "abcf";
    string s2 = "abz";
    if(s1 == s2){
        cout<<"Same"<<endl;
    }else{
        cout<<"Not same"<<endl;
    }
    if(s1 > s2){
        cout<<"S1 is greater"<<endl;
    }else{
        cout<<"S2 is greater"<<endl;
    }


    return 0;
}
